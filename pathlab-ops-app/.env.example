# Dev (SQLite, no server needed)
DATABASE_URL="file:./dev.db"
# Production (Postgres) - also switch provider in prisma/schema.prisma
# DATABASE_URL="postgresql://user:pass@host:5432/pathlab?schema=public"
